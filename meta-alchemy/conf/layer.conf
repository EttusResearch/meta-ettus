# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-alchemy-layer"
BBFILE_PATTERN_meta-alchemy-layer = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-alchemy-layer = "6"

LAYERSERIES_COMPAT_meta-alchemy-layer = "zeus"
