header:
  version: 10
  includes:
    - kas/include/base-standard.yml
machine: qemuarm
target:
  - deployment-image
local_conf_header:
  "remove packagegroup-core-boot from rootfs":
    CORE_IMAGE_BASE_INSTALL:remove = "packagegroup-core-boot"
